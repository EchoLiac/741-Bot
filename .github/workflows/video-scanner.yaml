name: Video Scanner

on:
  workflow_dispatch:

concurrency:
  group: video-scanner
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run video scanner
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          DISCORD_WEBHOOK_VIDEOS: ${{ secrets.DISCORD_WEBHOOK_VIDEOS }}
          GEMINI_MODEL: gemini-3.6-flash
          MAX_TRANSCRIPT_CHARS: "60000"
        run: python video_scanner.py

      - name: Commit scanner state
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add seen_videos.json video_summaries/

          if git diff --staged --quiet; then
            echo "Keine Änderungen"
            exit 0
          fi

          git commit -m "Update video scanner state"

          git pull --rebase origin main || {
            echo "Rebase fehlgeschlagen – versuche normalen Pull"
            git pull origin main --no-edit || true
          }

          git push
